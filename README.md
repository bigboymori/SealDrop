# SealDrop
SealDrop is a CLI‑first tool for creating and opening locally‑encrypted .sdpkg packages for sensitive file exchange. It uses scrypt‑derived keys and AES‑256‑GCM, signs and verifies packages, rejects tampered or unsafe outputs, and exposes JSON output modes for automation.
